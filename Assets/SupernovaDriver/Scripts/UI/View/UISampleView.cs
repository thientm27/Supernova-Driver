using Imba.UI;

namespace SupernovaDriver.Scripts.UI.View
{
    public class UISampleView : UIView
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
